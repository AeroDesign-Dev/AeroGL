<Window x:Class="AeroGL.ReportNeracaWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:AeroGL"
        Title="Neraca (Preview)" Height="740" Width="1080"
        Background="#15152A" FontFamily="Consolas" FontSize="14"
        WindowStartupLocation="CenterOwner" SnapsToDevicePixels="True" UseLayoutRounding="True">

    <Window.Resources>
        <local:AmountStringConverter x:Key="ValStr"/>
        <local:AmountColorConverter x:Key="ValCol"/>
        <local:RupiahConverter x:Key="Rp"/>

        <Style x:Key="Hdr" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#A3D8FF"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="Margin" Value="0,10,0,4"/>
        </Style>

        <Style x:Key="Lbl" TargetType="TextBlock">
            <Setter Property="Foreground" Value="#9EEAF9"/>
            <Setter Property="Margin" Value="0,2,0,2"/>
        </Style>

        <Style x:Key="Val" TargetType="TextBlock" BasedOn="{StaticResource Lbl}">
            <Setter Property="TextAlignment" Value="Right"/>
        </Style>

        <Style x:Key="StrongVal" TargetType="TextBlock" BasedOn="{StaticResource Val}">
            <Setter Property="FontWeight" Value="Bold"/>
        </Style>

        <Style x:Key="Line" TargetType="Border">
            <Setter Property="BorderBrush" Value="#4444FF"/>
            <Setter Property="BorderThickness" Value="0,1,0,0"/>
            <Setter Property="Margin" Value="0,6,0,6"/>
        </Style>
    </Window.Resources>

    <Grid Margin="14">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Background="#2A2752" Padding="10" CornerRadius="6" Margin="0,0,0,10">
            <WrapPanel Orientation="Horizontal" VerticalAlignment="Center">
                <TextBlock Text="Tahun:" Foreground="#9EEAF9" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <TextBox x:Name="TxtYear" Width="72" Margin="0,0,15,0" VerticalContentAlignment="Center"/>

                <TextBlock Text="Bulan:" Foreground="#9EEAF9" VerticalAlignment="Center" Margin="0,0,8,0"/>
                <ComboBox x:Name="CboMonth" Width="160" SelectedIndex="0" VerticalContentAlignment="Center">
                    <ComboBoxItem Content="1 - Januari"/>
                    <ComboBoxItem Content="2 - Februari"/>
                    <ComboBoxItem Content="3 - Maret"/>
                    <ComboBoxItem Content="4 - April"/>
                    <ComboBoxItem Content="5 - Mei"/>
                    <ComboBoxItem Content="6 - Juni"/>
                    <ComboBoxItem Content="7 - Juli"/>
                    <ComboBoxItem Content="8 - Agustus"/>
                    <ComboBoxItem Content="9 - September"/>
                    <ComboBoxItem Content="10 - Oktober"/>
                    <ComboBoxItem Content="11 - November"/>
                    <ComboBoxItem Content="12 - Desember"/>
                </ComboBox>
            </WrapPanel>
        </Border>

        <Border Grid.Row="1" Background="#0E0F1D" CornerRadius="8" Padding="2">
            <ScrollViewer VerticalScrollBarVisibility="Auto">
                <Border Padding="24">
                    <Grid x:Name="Paper">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="Auto"/>
                            <RowDefinition Height="12"/>
                            <RowDefinition Height="*"/>
                            <RowDefinition Height="Auto"/>
                        </Grid.RowDefinitions>

                        <TextBlock Grid.Row="0" Text="{Binding Title}" 
                                   Foreground="#EAEAEA" FontWeight="Bold" FontSize="16"
                                   TextAlignment="Center" Margin="0,0,0,4"/>
                        <TextBlock Grid.Row="1" Text="{Binding Period}"
                                   Foreground="#B7C9FF" FontSize="13"
                                   TextAlignment="Center" Margin="0,0,0,6"/>

                        <Grid Grid.Row="3">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="AKTIVA LANCAR" Style="{StaticResource Hdr}"/>

                                <TextBlock Grid.Row="1" Text="KAS" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="1" Text="{Binding Kas, Converter={StaticResource ValStr}}" Foreground="{Binding Kas, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="2" Text="BANK" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="2" Text="{Binding Bank, Converter={StaticResource ValStr}}" Foreground="{Binding Bank, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="3" Text="PIUTANG DAGANG" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="3" Text="{Binding PiutangDagang, Converter={StaticResource ValStr}}" Foreground="{Binding PiutangDagang, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="4" Text="PERSEDIAAN BARANG" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="4" Text="{Binding Persediaan, Converter={StaticResource ValStr}}" Foreground="{Binding Persediaan, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="5" Text="PIUTANG KARYAWAN" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="5" Text="{Binding PiutangKaryawan, Converter={StaticResource ValStr}}" Foreground="{Binding PiutangKaryawan, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="6" Text="PAJAK DIBAYAR DIMUKA" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="6" Text="{Binding PajakDibayarDimuka, Converter={StaticResource ValStr}}" Foreground="{Binding PajakDibayarDimuka, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <Border Grid.Row="7" Style="{StaticResource Line}"/>
                                <TextBlock Grid.Row="8" Text="JUMLAH AKTIVA LANCAR" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="8" Text="{Binding JumlahAktivaLancar, Converter={StaticResource ValStr}}" Foreground="{Binding JumlahAktivaLancar, Converter={StaticResource ValCol}}" Style="{StaticResource StrongVal}"/>

                                <TextBlock Grid.Row="9" Text="AKTIVA TETAP" Style="{StaticResource Hdr}" Margin="0,12,0,4"/>

                                <TextBlock Grid.Row="10" Text="AKTIVA TETAP" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="10" Text="{Binding AktivaTetap, Converter={StaticResource ValStr}}" Foreground="{Binding AktivaTetap, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="11" Text="AKUMULASI PENYUSUTAN" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="11" Text="{Binding AkumulasiPenyusutanNeg, Converter={StaticResource ValStr}}" Foreground="{Binding AkumulasiPenyusutanNeg, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <Border Grid.Row="12" Style="{StaticResource Line}"/>
                                <TextBlock Grid.Row="13" Text="JUMLAH AKTIVA TETAP" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="13" Text="{Binding JumlahAktivaTetap, Converter={StaticResource ValStr}}" Foreground="{Binding JumlahAktivaTetap, Converter={StaticResource ValCol}}" Style="{StaticResource StrongVal}"/>
                            </Grid>

                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>

                                <TextBlock Grid.Row="0" Text="PASSIVA LANCAR" Style="{StaticResource Hdr}"/>

                                <TextBlock Grid.Row="1" Text="HUTANG DAGANG" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="1" Text="{Binding HutangDagang, Converter={StaticResource ValStr}}" Foreground="{Binding HutangDagang, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="2" Text="HUTANG BANK" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="2" Text="{Binding HutangBank, Converter={StaticResource ValStr}}" Foreground="{Binding HutangBank, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="3" Text="HUTANG LAIN - LAIN" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="3" Text="{Binding HutangLain, Converter={StaticResource ValStr}}" Foreground="{Binding HutangLain, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="4" Text="BIAYA YMH DIBAYAR" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="4" Text="{Binding BYMHDibayarNeg, Converter={StaticResource ValStr}}" Foreground="{Binding BYMHDibayarNeg, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="5" Text="PAJAK YMH DIBAYAR" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="5" Text="{Binding PYMHDibayar, Converter={StaticResource ValStr}}" Foreground="{Binding PYMHDibayar, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <Border Grid.Row="6" Style="{StaticResource Line}"/>
                                <TextBlock Grid.Row="7" Text="JUMLAH PASSIVA LANCAR" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="7" Text="{Binding JumlahPassivaLancar, Converter={StaticResource ValStr}}" Foreground="{Binding JumlahPassivaLancar, Converter={StaticResource ValCol}}" Style="{StaticResource StrongVal}"/>

                                <TextBlock Grid.Row="8" Text="MODAL" Style="{StaticResource Hdr}" Margin="0,12,0,4"/>

                                <TextBlock Grid.Row="9" Text="MODAL YANG DISETOR" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="9" Text="{Binding ModalDisetor, Converter={StaticResource ValStr}}" Foreground="{Binding ModalDisetor, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="10" Text="LABA (RUGI) DITAHAN" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="10" Text="{Binding LabaDitahan, Converter={StaticResource ValStr}}" Foreground="{Binding LabaDitahan, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <TextBlock Grid.Row="11" Text="LABA (RUGI) BERJALAN" Style="{StaticResource Lbl}"/>
                                <TextBlock Grid.Row="11" Text="{Binding LabaBerjalan, Converter={StaticResource ValStr}}" Foreground="{Binding LabaBerjalan, Converter={StaticResource ValCol}}" Style="{StaticResource Val}"/>

                                <Border Grid.Row="12" Style="{StaticResource Line}"/>
                            </Grid>
                        </Grid>

                        <Grid Grid.Row="4" Margin="0,14,0,0">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="36"/>
                                <ColumnDefinition Width="*" />
                            </Grid.ColumnDefinitions>

                            <Grid Grid.Column="0">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="260" />
                                </Grid.ColumnDefinitions>
                                <TextBlock Grid.Column="0" Text="TOTAL AKTIVA" Style="{StaticResource Lbl}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Column="1" Text="{Binding TotalAktiva, Converter={StaticResource ValStr}}" Foreground="{Binding TotalAktiva, Converter={StaticResource ValCol}}" Style="{StaticResource StrongVal}" VerticalAlignment="Center"/>
                            </Grid>

                            <Grid Grid.Column="2">
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="10"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="260" />
                                </Grid.ColumnDefinitions>

                                <TextBlock Grid.Row="0" Grid.Column="0" Text="JUMLAH MODAL" Style="{StaticResource Lbl}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Row="0" Grid.Column="1" Text="{Binding JumlahModal, Converter={StaticResource ValStr}}" Foreground="{Binding JumlahModal, Converter={StaticResource ValCol}}" Style="{StaticResource StrongVal}" VerticalAlignment="Center"/>

                                <TextBlock Grid.Row="2" Grid.Column="0" Text="TOTAL PASSIVA" Style="{StaticResource Lbl}" VerticalAlignment="Center"/>
                                <TextBlock Grid.Row="2" Grid.Column="1" Text="{Binding TotalPassiva, Converter={StaticResource ValStr}}" Foreground="{Binding TotalPassiva, Converter={StaticResource ValCol}}" Style="{StaticResource StrongVal}" VerticalAlignment="Center"/>
                            </Grid>
                        </Grid>
                    </Grid>
                </Border>
            </ScrollViewer>
        </Border>

        <Grid Grid.Row="2" Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TxtInfo" Grid.Column="0" Foreground="#AAA" VerticalAlignment="Center" Text="Siap." TextTrimming="CharacterEllipsis"/>

            <StackPanel Grid.Column="1" Orientation="Horizontal">
                <Button x:Name="btnShow" Content="Tampilkan (Enter)" Click="BtnShow_Click" 
                        Margin="0,0,8,0" Height="30" Padding="10,0" 
                        Background="#444466" Foreground="White" BorderBrush="#666"/>

                <Button Content="Print (P)" Click="BtnPrint_Click" 
                        Margin="0,0,8,0" Height="30" Padding="10,0" 
                        Background="#2D8F6F" Foreground="White" BorderBrush="#2D8F6F"/>

                <Button Content="Tutup (Esc)" Click="BtnClose_Click" 
                        Height="30" Padding="10,0" 
                        Background="#FF6060" Foreground="White" BorderBrush="#FF6060"/>
            </StackPanel>
        </Grid>

    </Grid>
</Window>